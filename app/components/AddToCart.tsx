'use client'
const AddToCart = () => {
    return (
        <div>
            <button className="btn  btn-primary" onClick={() => console.log('hello akaid')
            }>Hello </button>
        </div>
    );
};

export default AddToCart;