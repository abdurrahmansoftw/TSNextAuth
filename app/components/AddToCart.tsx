'use client'
import React from 'react';

const AddToCart = () => {
    return (
        <div>
            <button onClick={() => console.log('helo')}> hello </button>
        </div>
    );
};

export default AddToCart;